import React from "react";
import { Box } from "@chakra-ui/core";

interface FollowCardProps {}

export const FollowCard: React.FC<FollowCardProps> = ({}) => {
  return <Box py={2}>adsf</Box>;
};
